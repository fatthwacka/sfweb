# Docker Compose Override Example
# Copy this file to docker-compose.override.yml to customize ports

version: "3.7"

services:
  postgres:
    ports:
      - "5433:5432"  # Alternative PostgreSQL port

  app:
    ports:
      - "3001:5000"  # Alternative app port

  adminer:
    ports:
      - "8081:8080"  # Alternative Adminer port

# Usage:
# 1. Copy this file: cp docker-compose.override.yml.example docker-compose.override.yml
# 2. Edit ports as needed
# 3. Run: npm run docker:dev
# 4. Access app at: http://localhost:3001 (or your chosen port)